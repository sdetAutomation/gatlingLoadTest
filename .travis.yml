#os: osx
#before_install:
#- brew upgrade maven

language: java
sudo: true
script:
- mvn gatling:execute -Dgatling.simulationClass=computerdatabase.BasicSimulation
- mvn -DnumOfUser=20  gatling:execute -Dgatling.simulationClass=jsonPlaceholder.GetExample
- mvn -DnumOfUser=20  gatling:execute -Dgatling.simulationClass=jsonPlaceholder.PostExample
